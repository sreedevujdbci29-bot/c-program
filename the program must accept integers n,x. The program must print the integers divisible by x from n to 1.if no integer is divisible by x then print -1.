#include<stdio.h>
#include<stdlib.h>

int main()
{
int n,x;
int d=0;
scanf("%d %d",&n,&x);
for(int i=n;i>=1;i--){
    if(i%x!=0){
        printf("%d ",i);
        d++;
    }
}
if(d==0){
    printf("-1");
}
}
